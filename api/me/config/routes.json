{
  "routes": [
    {
      "method": "GET",
      "path": "/me",
      "handler": "me.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/me",
      "handler": "me.update",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "DELETE",
      "path": "/me",
      "handler": "me.delete",
      "config": {
        "policies": ["global::isAdmin"]
      }
    }
  ]
}
