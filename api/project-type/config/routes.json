{
  "routes": [
    {
      "method": "GET",
      "path": "/project-types",
      "handler": "project-type.find",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "GET",
      "path": "/project-types/count",
      "handler": "project-type.count",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "GET",
      "path": "/project-types/:id",
      "handler": "project-type.findOne",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "POST",
      "path": "/project-types",
      "handler": "project-type.create",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "PUT",
      "path": "/project-types/:id",
      "handler": "project-type.update",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "DELETE",
      "path": "/project-types/:id",
      "handler": "project-type.delete",
      "config": {
        "policies": ["global::isAdmin"]
      }
    }
  ]
}
