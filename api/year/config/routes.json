{
  "routes": [
    {
      "method": "GET",
      "path": "/years",
      "handler": "year.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/years/count",
      "handler": "year.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/years/:id",
      "handler": "year.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/years",
      "handler": "year.create",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "PUT",
      "path": "/years/:id",
      "handler": "year.update",
      "config": {
        "policies": ["global::isAdmin"]
      }
    },
    {
      "method": "DELETE",
      "path": "/years/:id",
      "handler": "year.delete",
      "config": {
        "policies": ["global::isAdmin"]
      }
    }
  ]
}
