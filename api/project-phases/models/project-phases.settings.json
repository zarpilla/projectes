{
  "kind": "collectionType",
  "collectionName": "project_phases",
  "info": {
    "name": "ProjectPhases"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "incomes": {
      "via": "project_phase",
      "collection": "phase-income"
    },
    "expenses": {
      "via": "project_phase",
      "collection": "phase-expense"
    },
    "project": {
      "via": "project_phases",
      "model": "project"
    }
  }
}
