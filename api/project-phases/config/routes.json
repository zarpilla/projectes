{
  "routes": [
    {
      "method": "GET",
      "path": "/project-phases",
      "handler": "project-phases.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-phases/count",
      "handler": "project-phases.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-phases/:id",
      "handler": "project-phases.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-phases",
      "handler": "project-phases.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-phases/:id",
      "handler": "project-phases.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-phases/:id",
      "handler": "project-phases.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
